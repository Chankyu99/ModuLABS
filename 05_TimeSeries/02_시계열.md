# 시계열 데이터를 표현하는 그래프

EuStockMarkets 시계열 데이터셋을 이용해 Matplotlib로 4가지 방법으로 시각화를 한다.

## Line Plot

간단한 선을 그려 데이터의 시간과 순서에 따라 어떻게 변하는지 기본적으로 시계열 파악 시 활용된다.

![[Pasted image 20260414235617.png]]

## Historgram

데이터의 분포를 판단할 수 있으며, 일반적으로 시계열 데이터는 1차 차분에 대해 히스토그램을 그리면 특정한 분포를 볼 수 있다.

![[Pasted image 20260414235802.png]]

## Scatter Plot

두 시계열의 관계에 대한 유의미한 정보를 확인할 수 있다. 데이터를 차분하면 새로운 의미를 찾을 수 있다.

![[Pasted image 20260414235844.png]]

## Circular Chart

시계열에 대한 유의미한 인사이트를 얻긴 어렵지만, 시각적 효과를 위해 사용한다. 계절성을 시각화할 땐 가끔 유용하다.

![[Pasted image 20260414235926.png]]


# EuStockMarket 데이터셋 탐색

DAX, SMI, CAC, FTSE와 같은 주요 유럽 주가지수의 일자별 종가 데이터를 확인할 수 있다.

### Line Plot

추세를 확인해보자.

![[Pasted image 20260415000327.png]]
### Histogram

SMI column과 차분을 적용한 SMI column의 차이를 히스토그램으로 비교해보았다.

차분을 통해 분포의 평균이 일정하고, 정상성을 가지게 되었으며 시각적으로도 가운데로 밀집한 형태의 히스토그램으로 변경되었다.

### Scatter Plot

차분이 적용되고 안되고의 차이가 산점도에서도 명확히 확인된다.

# 시계열에 특화된 EDA 방법

## ACF(AutoCorrelation Function) Plot

AutoCorrelation, 자기상관은 시계열 데이터에서 일정 간격이 있는 값들 사이의 상관관계를 의미한다.

이것을 함수로 나타내어 시간에 따른 상관 정도를 나타내는 그래프가 바로 ACF Plot이다.

![[Pasted image 20260415000654.png]]

일반적인 사인함수와 사인함수의 ACF Plot인데, 임계값이 파란색으로 나타나있다.

## PACF(Partial AutoCorrelation Function) Plot

P가 붙어 편자기상관이라는 뜻으로, 자신에 대한 그 시차의 편상관을 의미한다. 

즉, 두 시점사이의 전체 상관관계에서 그 사이 다른 시점의 조건부 상관관계를 뺀 것!

![[Pasted image 20260415000907.png]]

ACF에서 봤던 사인함수를 PACF로 나타내자, ACF에서 불필요한 중복 관계가 제거된 것을 볼 수 있다.

> ACF와 PACF 차이점 : ACF는 두 시점간 상관관계 계산 시 두 시점 사이 모든 lag에 대한 정보가 들어감. PACF는 오로지 두 시점만의 상관관계만을 계산해 다른 lag는 조건부 상관관계로 제거된다.

직접 코드를 짜보며 시각화된 결과를 확인해보자. 

statsmodels 라이브러리에  plot_acf, plot_pacf 모듈을 임포트해 간단히 사용할 수 있다.     

일반적인 사인함수를 간단히 그려보면,

![[Pasted image 20260415001752.png]]

이걸 acf, pacf로 표현하면 다음과 같이 나온다.
![[Pasted image 20260415001816.png]]
![[Pasted image 20260415001819.png]]
ACF를 보면 lag가 진행됨에 따라 상관계수가 0으로 수렴하지 않고, 특정 시차마다 파동을 그리며 다시 커지는 패턴이 반복된다. 데이터에 일정한 주기가 있음을 시사한다.

정상적인 시계열이라면 ACF가 초기에 급감해야하는데, 파랗게 칠해진 신뢰 구간을 넘는 값이 많으며 전체적으로 값이 크다. 이는 평균이나 분산이 시간에 따라 일정하지 않다는 뜻으로, 정상성을 가지고 있다고 할 수 없다. 

PACF를 보면 역시 불규칙하게 튀는 지점이 많다. 데이터의 추세나 계절성이 제거되지 않았을 때 전형적으로 나타나는 모습이다.

> 시계열 EDA에서 체크해야할 가장 중요한 위험은 ‘허위 상관’ (spurious correlation)이며 이것은 두 개 이상의 변수가 통계적 상관은 있지만 인과관계가 없는 관계를 말한다.