# pip install 필요한 라이브러리 목록
# 가상환경에서 아래 명령어로 설치하세요:
#   .venv/bin/pip install -r requirements.txt

# 1. 화면 그리는 용도 (app.py)
streamlit

# 2. RAG 파이프라인 뼈대 (bot_logic.py, ingest.py)
langchain
langchain-community
langchain-core

# 3. OpenAI 모델 및 임베딩 (gpt-4o-mini, text-embedding-3-small)
langchain-openai
openai
tiktoken

# 4. 벡터 데이터베이스 (ChromaDB)
langchain-chroma
chromadb

# 5. 환경변수 관리
python-dotenv